// Criando função 
function nomeCompleto (nome, sobrenome) {
  console.log(`${nome} ${sobrenome}`);
}

// Executando a função nomeCompleto
nomeCompleto('Raíza', 'Cirne');
nomeCompleto('Ana', 'Cozza');