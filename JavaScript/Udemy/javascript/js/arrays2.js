// array -> objeto dentro do JavaScript que você pode adicionar múltiplos valores. Esses valores podem ser números, booleans, strings. 

const friends = ['Marcos', 'Ana', 'Marcela']; // array tipo string
const num = [1, 2, 3, 4, true]; //array tipo number e boolean: true

console.log(friends); 
console.log(num); 

// Array é a solução para adicionar vários valores a uma variável. 