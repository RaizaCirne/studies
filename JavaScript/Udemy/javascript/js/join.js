// join: add qualquer coisa que deseja a uma array
let clients = ['Andre', 'Jose', 'Marcela']; 
console.log(clients);

// Passar o que será adicionado em cada item da array
let clientsJoin = clients.join(', '); // separador
console.log(clientsJoin); 

// Usabilidade: Converter array para string sem add itens