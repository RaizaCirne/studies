/*
Antes de começar a construir códigos do zero, verifique se já tem um objeto já construído dentro do Javascript que pode resolver ou então ajudar a solução do seu problema.
*/


// Math.max() retorna o maior valor
console.log(Math.max(1, 3, 15, 2, 7, 4));

// Math.min() retorna o menor valor
console.log(Math.min(1, 3, 15, 2, 7, 4));

// Math.round() retorna o valor inteiro -> 5
console.log(Math.round(5.3));

// Math.PI retorna o valor de PI
console.log(Math.PI);